/** Dispatched on `window` after liked-events storage changes (same-tab). */
export const REKINDLE_LIKED_EVENTS_CHANGED = 'rekindle:liked-events-changed';

/** Dispatched after profile fields are saved (bio, name, avatar, etc.). */
export const REKINDLE_PROFILE_UPDATED = 'rekindle:profile-updated';
